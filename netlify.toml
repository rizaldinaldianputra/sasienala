# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Pakai Node versi spesifik yang valid di Netlify
  NODE_VERSION = "20.19.0"

# Optional: redirect root ke index.html (untuk SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
